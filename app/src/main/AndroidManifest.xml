<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myfirstapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UploadActivity" />
        <activity android:name=".Search_Other_Users_Activity" />
        <activity android:name=".Loading_Screen_Activity" />
        <activity android:name=".Change_Password_Activity" />
        <activity android:name=".Profile_First_Setup" />
        <activity android:name=".Profile_Settings_Activity" />
        <activity android:name=".Forgot_Password_Activity" />
        <activity android:name=".Account_Info_Activity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>